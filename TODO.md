# TODO

## Goal
Ensure the `uv` environment is up to date and all CI verification checks (including `codespell`) pass.

## Tasks

### 1. Environment Update
- [x] Run `uv sync` to update the lockfile and environment.

### 2. Verification
- [ ] Run `ruff` for linting and formatting.
- [ ] Run `pytest` to ensure all tests pass.
- [ ] Run `codespell` to check for typos.
- [ ] Run `mkdocs build` to verify documentation.
